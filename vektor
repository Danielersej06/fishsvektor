
class Vector:
    def init(self, x, y):
        self.x = x
        self.y = y

    def add(self, other):
        return Vector(self.x + other.x, self.y + other.y)

    def str(self):
        return f"({self.x},{self.y})"


v1 = Vector(3,4)
v2 = Vector(1,1)

print(v1.add(v2).str())